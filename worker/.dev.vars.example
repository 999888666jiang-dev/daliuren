# Copy to project-root .dev.vars for local development only. Never commit .dev.vars.
# Real secrets are entered by the owner, never added to wrangler.jsonc or VITE_*.
DEEPSEEK_API_KEY=
ALLOW_LOCAL_DEV=true
AI_ENABLED=true
